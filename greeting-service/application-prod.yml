spring:
  application:
    name: greeting-service
greeting-service:
  greeting: "Hello"
  default-value: "World Prod"
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
  endpoint:
    health:
      show-details: always
